# FastWAM 使用 UniVTAC 数据微调计划

## 1. 目标与执行约束

目标是使用 `/media/hzm/data24t/GitHub/UniVTAC` 中的 factory peg insertion 仿真数据微调 FastWAM，并在 UniVTAC 环境中通过固定、未见过的 seed 进行闭环评估。

当前 LIBERO 完整评估继续运行并保持后台监控。评估完成后必须暂停，释放 GPU；在收到用户后续明确指令前，不得自动启动 UniVTAC 数据生成、数据转换、微调或评估任务。

实施期间遵守以下约束：

- 不覆盖 UniVTAC 原始数据和已有实验结果。
- 不把失败、损坏或重复轨迹静默加入训练集。
- 训练集、验证集和最终测试集按 episode/seed 隔离，禁止按帧随机拆分。
- 训练和离线误差不能替代闭环成功率。
- 所有长时间任务必须由后台 supervisor 监控，并保存可恢复状态。
- 数据或控制契约发生变化时，必须重新完成回放验证。

## 2. 当前条件与主要结论

FastWAM 支持 LeRobot 2.1 和 LeRobot 3.0 数据集。当前发布的 LIBERO 模型使用两路 RGB 图像、7 维动作和 8 维状态；checkpoint 中的 MoT 约有 60.2 亿参数，文件约 11.21 GiB。

UniVTAC factory 数据当前具有以下结构：

- 原始数据格式为 HDF5，训练侧还存在 ACT 格式的 HDF5。
- 状态为 8 维 qpos：7 个机械臂关节和 1 个夹爪值。
- 动作为 8 维绝对 qpos。
- 视觉包含 `cam_high`、左触觉和右触觉。
- 触觉可使用左右传感器的 `rgb_marker` 图像。
- 数据采样频率为 15 Hz。
- 已整理的 `data/factory_peg_insert/pi05_train_100` 包含 100 条轨迹、约 2757 个动作样本，每条约 27～28 帧。

现有 100 条数据足够验证转换和训练链路，但不足以证明泛化能力。此前 UniVTAC 的 100 条数据训练结果表现为离线误差较低、闭环插孔失败，因此后续必须使用严格闭环评估决定是否补充数据。

## 3. 模型输入与控制契约

### 3.1 推荐动作表示

正式方案使用 UniVTAC 原生 8 维绝对 qpos：

```text
[joint_1, joint_2, joint_3, joint_4, joint_5, joint_6, joint_7, gripper]
```

该方案能够精确回放现有专家轨迹，避免把四元数转换为末端姿态增量时产生约定错误和累计误差。

FastWAM 需要执行以下适配：

1. 将 `action_dim` 和 `action_output_dim` 设置为 8。
2. 保持 `proprio_dim` 为 8。
3. 从当前 LIBERO checkpoint 加载尺寸兼容的参数。
4. 跳过并重新初始化所有依赖动作维度的输入层和输出头。
5. 输出加载报告，列出成功加载、重新初始化和意外缺失的参数。
6. 对意外缺失参数采用失败退出，不能静默继续。

虽然 7 维末端增量与 LIBERO 动作维度相同，但动作语义并不相同，因此不以表面维度一致作为首选方案。

### 3.2 状态表示

状态使用 8 维 qpos，与动作保持同一关节顺序和夹爪定义。转换器必须验证：

- 关节顺序在全部 episode 中一致。
- 夹爪值方向、范围和部署接口一致。
- 状态和动作没有 NaN/Inf。
- normalization 只使用训练集计算。

### 3.3 视觉和触觉输入

先建立两个可比较版本：

| 版本 | 输入 | 用途 |
| --- | --- | --- |
| V | `cam_high` | 视觉基线 |
| VT | `cam_high`、`tac_left/rgb_marker`、`tac_right/rgb_marker` | 视觉触觉模型 |

每路图像缩放到 224×224，保持相同颜色空间、通道顺序和归一化方式。VT 版本使用 FastWAM 已有的多相机机制，不在转换阶段把左右触觉不可逆地压缩为一个低分辨率拼图。

如果单卡显存无法容纳三路输入，可额外测试两路降配版本：`cam_high` 加左右触觉拼图。该版本只能作为单卡资源受限方案，不能替代 VT 正式对照实验。

### 3.4 时序与语言指令

- 数据和部署控制频率统一为 15 Hz。
- 使用 33 步动作窗口，短轨迹必须生成正确的 padding mask。
- padding 动作不能参与 loss。
- 每个 episode 使用任务指令 `insert the peg into the hole`。
- 转换时保存原始 seed、源文件路径、源 SHA256 和转换配置摘要。

## 4. 阶段一：转换现有 100 条数据

实现独立的 UniVTAC HDF5 到 LeRobot 3.0 转换程序，输出到 FastWAM 下的新目录，不修改 UniVTAC 原文件。

转换器需要完成：

1. 读取状态、动作、外部相机和左右触觉图像。
2. 写入 episode index、frame index、timestamp、task index 和语言指令。
3. 保留 15 Hz 时间基准。
4. 生成训练、验证 episode 清单和数据指纹。
5. 生成图像视频、parquet 数据和 LeRobot 3.0 metadata。
6. 生成转换报告，包括丢弃轨迹及其原因。

转换验收门槛：

- 100 条输入轨迹全部可读取；若有轨迹被拒绝，必须明确报告。
- 时间戳严格递增。
- 图像、状态和动作长度一致。
- 动作和状态均为有限的 8 维数值。
- 不存在重复 episode 指纹。
- 随机抽取至少 20 条转换后动作进行 UniVTAC 回放。
- 专家回放稳定成功率不低于 95%。

如果回放未通过，应先修复采样频率、动作对齐、夹爪定义或部署接口，不能进入训练。

## 5. 阶段二：训练链路 Smoke Test

使用现有 100 条数据验证完整工程链路：

1. 构造 V 和 VT 数据配置。
2. 验证数据加载张量维度、padding mask 和 episode 边界。
3. 预计算语言 embedding。
4. 加载 LIBERO checkpoint，生成参数迁移报告。
5. 使用 8 个固定样本执行小样本过拟合测试。
6. 执行 20～100 个训练 step。
7. 保存 checkpoint 并恢复，验证恢复前后固定输入输出一致。

Smoke Test 必须满足：

- 无 NaN/Inf。
- loss 能在固定小样本上明显下降。
- padding 区域不贡献 loss。
- checkpoint 能完整恢复。
- GPU 显存没有随 step 持续增长。
- 数据加载和训练日志不存在被忽略的异常。

该阶段只证明链路正确，模型结果不能作为正式结论。

## 6. 阶段三：训练资源方案

当前机器只有一张 NVIDIA GeForce RTX 5090 32GB。现有 FastWAM 训练器会训练完整 MoT，因此不能直接在该卡上进行原配置的全量微调。

### 6.1 多卡方案

若可使用至少 8 张 32GB GPU：

- 复用项目现有 DeepSpeed ZeRO 训练入口。
- 先训练动作专家和动作相关模块。
- 根据闭环结果决定是否解冻更多视频模块。
- V 和 VT 使用相同 seed、训练步数和有效 batch 做公平比较。

### 6.2 当前单卡方案

若仅使用当前 RTX 5090：

- 冻结视频专家、VAE 和非动作模块。
- 对 ActionDiT 和动作相关层使用 LoRA。
- 使用 bf16、gradient checkpointing、batch size 1 和梯度累积。
- 必要时预计算视频 latent，并卸载训练时不需要的编码模块。
- 首先执行 20-step 显存门槛测试；只有显存稳定且留有安全余量才继续。

项目目前没有现成的 LoRA/PEFT 支持。引入新依赖前必须获得明确批准，并完成依赖与许可证检查。

## 7. 阶段四：按学习曲线补充仿真数据

采用逐级扩充，避免一开始盲目生成大量同分布数据：

| 阶段 | 成功轨迹数 | 目的 |
| --- | ---: | --- |
| A | 100 | 转换、回放和训练链路验证 |
| B | 300 | 首次有统计意义的闭环评估 |
| C | 600 | 判断增加数据是否继续改善泛化 |
| D | 1000 | 窄任务正式训练集 |
| E，可选 | 2000 | 仅在 1000 条时仍明显受数据量限制时执行 |

现有稳定 factory 采集的单条成功轨迹中位耗时约 38 秒。从 100 条扩充到 1000 条需要约 900 条新轨迹，纯仿真时间约 13 小时；考虑模拟器启动、重试和验证，初步预计 18～30 小时。实际 ETA 应在新增 50 条完成后使用实测吞吐重新计算。

磁盘当前约有 20TB 可用空间，数据容量不是主要限制。新数据应写入独立、不可覆盖的目录。

### 7.1 Seed 隔离

- 训练候选 seed：从现有数据末尾继续，例如 110 起。
- 离线验证集：从成功轨迹中按 seed 固定保留约 10%。
- 调参闭环集：固定使用 10000～10099。
- 最终闭环集：固定使用 20000～20199，在最终模型确定前不得查看结果。

### 7.2 数据生成质量门槛

每条训练轨迹必须满足：

- `stable_success=true`。
- `bore_axis_envelope_ok=true`。
- 无滑落。
- 图像全部可解码。
- 状态和动作无 NaN/Inf。
- 轨迹状态指纹不重复。
- 不包含 Newton、CUDA、OOM 或 solver failure。

单个 seed 最多重试 2 次。连续 3 个 seed 都失败时停止采集并保留现场，查明原因后再继续。

先保持现有 ±3mm 初始对齐随机化。得到稳定基线后，再单独增加位置、姿态、摩擦和视觉随机化。每一种数据分布都必须记录到 manifest，不能无标记混合。

### 7.3 是否继续补数据

100、300、600 和 1000 条模型使用相同的闭环种子评估。若数据量大幅增加后成功率提升不足 3 个百分点，应暂停数据生成，优先检查动作执行、模型容量、观测设计和训练配置。

## 8. 阶段五：正式微调

推荐训练顺序：

1. 使用 100 条数据训练 V 和 VT 工程基线。
2. 扩充到 300 条并重新训练，不在旧数据实验上直接追加未经记录的数据。
3. 根据固定闭环结果决定是否扩充到 600 和 1000 条。
4. 每个正式配置至少使用 3 个训练随机种子。

初始训练配置建议：

| 参数 | 建议值 |
| --- | --- |
| normalization | 使用当前 UniVTAC 训练集重新计算 |
| LoRA/动作头学习率 | `1e-4` 起步 |
| 完整 ActionDiT 学习率 | `1e-5` 起步 |
| 有效 batch size | 8～16 |
| checkpoint 间隔 | 每 200 optimizer step |
| 闭环评估间隔 | 每 500 step 或每个 epoch |
| 混合精度 | bf16 |
| early stopping | 固定验证 seed 的闭环成功率 |

训练集变化后必须重新计算 normalization stats，并把数据指纹写入 checkpoint metadata。不得使用 LIBERO normalization stats。

## 9. 阶段六：闭环评估与验收

正式评估至少使用 100 个固定、未见过的 seed；最终评估使用独立的 200 个 seed。报告以下指标：

- `stable_success` 成功率。
- XY 误差。
- Z 误差。
- 倾斜角。
- 滑落率。
- 平均完成时间。
- 仿真异常率。
- 失败类型：对齐失败、滑落、姿态发散、未插入和仿真异常。

建议最终验收门槛：

- 100 个调参闭环 seed 中至少 80 个稳定成功。
- 仿真异常率低于 1%。
- 3 个训练种子的成功率标准差不超过 5 个百分点。
- VT 成功率不低于 V；只有稳定优于 V 时，才认定触觉输入有效。
- 最终 200 个隔离 seed 的结果完整，无选择性重跑。

每次评估必须固定 checkpoint、normalization stats、环境配置和数据指纹。

## 10. 后台监控与恢复

采集、转换、训练和评估统一由 supervisor 管理：

- 每 10 秒记录进程 PID、状态和 heartbeat。
- 每 30 秒记录 GPU 利用率、显存和温度。
- 持续记录当前阶段、seed、已完成 episode、训练 step 和最近 checkpoint。
- 进程退出后立即检查退出码、日志尾部和阶段产物。
- 只对明确可恢复的异常自动重试。
- NaN、数据损坏、未知参数缺失和连续 solver failure 必须停止任务。
- 训练只能从完整 checkpoint 恢复，不能跳过失败 batch 来制造完成状态。
- 每个阶段维护 `status.json`、配置副本、日志和输入输出 SHA256。
- 完成最终评估后自动停止 GPU 工作负载，不自动进入下一阶段。

## 11. 实施顺序和阶段门槛

1. 等当前 LIBERO 完整评估结束并释放 GPU，然后暂停。
2. 实现 HDF5 到 LeRobot 3.0 转换器和完整性检查。
3. 对现有 100 条轨迹进行转换和至少 20 条精确回放验证。
4. 修改 FastWAM，支持 8 维动作 checkpoint 迁移和明确的参数加载报告。
5. 完成数据加载、小样本过拟合和 checkpoint 恢复测试。
6. 确定单卡 LoRA或多卡训练路线。
7. 使用 100 条数据训练 V/VT 工程基线并闭环评估。
8. 依次扩充到 300、600、1000 条，并在每一级重新评估。
9. 选择 3 个训练种子的正式模型。
10. 在 200 个完全隔离的 seed 上完成最终评估。

任何阶段未达到门槛时，停止进入下一阶段，保留产物和日志并定位根因。

## 12. 预期变更范围与审批点

预计需要修改或新增：

- UniVTAC 到 LeRobot 3.0 的转换与验证程序。
- FastWAM 的 UniVTAC 数据配置和任务配置。
- 8 维动作 checkpoint 迁移逻辑。
- UniVTAC 环境中的 FastWAM 部署适配器。
- 长任务 supervisor、状态文件和结果汇总程序。
- 单卡路线所需的 LoRA 支持。

涉及的新依赖、跨仓库接口和任何控制或安全限制变化，必须在执行前单独检查。仿真数据生成和评估不得连接或驱动真实机器人。
